# PLAN — <task name>

> The source of truth for **intent**. Written once before execution. Re-read at the top of
> every iteration to re-anchor and defeat semantic drift. Lives in the repo so it survives
> context compaction.

## Goal (one sentence)
<What success looks like, in the user's terms. If you can't state it in one sentence, you
don't understand the task yet.>

## Acceptance criteria (the bar)
- [ ] <Objective, checkable. Prefer computational checks: "test X passes", "type-checks",
      "endpoint returns 200" over "looks good".>
- [ ] <…>
- [ ] <…>

## Out of scope (explicit non-goals)
- <Things a reasonable agent might drift into but must NOT do.>

## Constraints
- <Stack, style, perf, security, compatibility, deadlines, budget.>

## Grounded feedback signal
<What will tell us, every iteration, whether we're on track? Tests / compiler / metric /
rubric / isolated critic. If there is none, define one before starting.>

## Milestones
1. **M1 — <name>** — <done-when, verifiable>
2. **M2 — <name>** — <done-when, verifiable>
3. **M3 — <name>** — <done-when, verifiable>

## Open questions / decisions to make
- <Question> → <decision, once made, with one-line rationale>

## Human gates
- [ ] Plan approved before execution
- [ ] Review before <irreversible action: write / send / deploy / merge>
